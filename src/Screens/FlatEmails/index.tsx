import React from 'react';
import {
    View,
    Text,
    FlatList,
    ScrollView
} from 'react-native';

export function FlatEmails() {

    const emails = [
        id: string;
        to: string;
        tittle: string;
        summary: string;
        time: string;
        star: boolean;
        picture: URL;
    ]

    return (
        <View>
            <ScrollView>
                <FlatList>

                </FlatList>
            </ScrollView>
        </View>
    )
}